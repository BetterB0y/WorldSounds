package pl.polsl.worldsounds.ui.resources

import androidx.compose.foundation.shape.RoundedCornerShape

object S {
    val large = RoundedCornerShape(D.Radius.large)
    val dialog = RoundedCornerShape(D.Radius.dialog)
}
